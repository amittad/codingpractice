let a={
    amit:20,
    ajit:20,
    aadesh:30,
}
for(let i=0;i<Object.keys(a).length;i++){
    if(Object.keys(a)[i]=="ajit"){
        a[i]="bhau";
    }
    for (let j = 0; j <Object.keys(a).length; j++) {
        
       }
}
   //for (let j = 0; j <Object.keys(a).length; j++) {
   // console.log(Object.keys(a)[j])
